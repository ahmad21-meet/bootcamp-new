function calcAverage (arr){
    var sum  = 0;
    for ( let i = 0 ; i < arr.length; i++ ){
    sum += arr[i];
    }
     return console.log(sum); 
    }
    calcAverage (arr =[ 85 , 90 , 92 ]);

    // we should but var sum = 0;