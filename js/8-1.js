const book = {

    name : "how to make money 20 ways",
    author:"ahmad",
    year: "2020",
    type: "drama"


};
const rock = () =>{

    console.log (` The book ${book.name} was written by ${book.author} in the year ${book.year} and the type is ${book.type}`);

}

rock(book);